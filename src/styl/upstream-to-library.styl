// Upstream staging for nm-stylus-library.
// Shared styles are drafted/overridden here, then promoted into the library and
// removed once a release ships them. Add new candidates below.

// ── Promoted to nm-stylus-library v0.14.0 (2026-06) ──────────────────────────
// Local overrides removed; the library now provides these:
//   - $container-* Stylus vars
//   - .ui-tag dot sized 0.7em (was 1cap)
//   - .text-overflow-ellipsis (valid white-space/overflow/text-overflow)
//   - .ui-button--gray
//   - .grid-row--nested / .grid-row--nested-tight (canonical names)
//   - .ui-embed-container (+ --4-3 / --1-1 ratio modifiers, aspect-ratio impl)
//   - .ui-rounded-box--nested
//   - .ui-border* on new --color-gray-light + .ui-border--gray-mid (via --ui-border-color)
// (v0.14.1 patched a deprecation-comment leak that broke CSS minification.)
//
// Deprecated aliases still in the library, scheduled for removal in v0.15.0:
//   .grid--nested(-tight) → .grid-row--nested(-tight)
//   .u-video-embed-container / .ui-responsive-video-container → .ui-embed-container
//   .ui-rounded-image → .ui-rounded-box
//   .ui-rounded-box--large → .ui-rounded-box--nested
// This theme's usages were migrated off all of the above in the 0.14.1 bump, so
// the aliases are safe to delete next library cycle.
